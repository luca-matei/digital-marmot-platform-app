"use client";
import { useState, ReactNode } from 'react';
import Typography from '@mui/material/Typography';

interface FinancesProps {
  children: ReactNode;
}

export default function Finances({ children }: FinancesProps) {
  return (children);
}
